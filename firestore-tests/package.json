{
  "name": "drop-firestore-rules-tests",
  "private": true,
  "type": "module",
  "description": "Security-rules tests for firestore.rules, run against the Firestore emulator. Deliberately NOT part of functions/ — these dependencies must never ship in a Cloud Functions bundle.",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project drop-rules-tests --config ../firebase.json \"node --test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0"
  }
}
