<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- GPS-verified attendance clock in/out. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- FCM push delivery. INTERNET is required to reach FCM; POST_NOTIFICATIONS
         is the Android 13+ (API 33) runtime permission WITHOUT WHICH no push is
         ever shown — `FirebaseMessaging.requestPermission()` surfaces the OS
         prompt, but the permission must be declared here or the grant is a
         no-op and every notification is silently dropped. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Reading the connectivity state for the online-only gate
         (`ConnectivityGate`). connectivity_plus declares this in its own
         manifest and the merger would supply it either way; it is declared here
         because this app's permissions are documented in one place. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- Auto Backup is OFF — Firebase Auth session persistence on Android.
         Firebase Auth persists the signed-in session in this app's private
         SharedPreferences (unlike iOS, which keeps it in the Keychain). With
         Android Auto Backup left at its default (`allowBackup=true`, which is
         what the merged manifest resolves to when unset), that auth store — plus
         the `flutter_secure_storage` EncryptedSharedPreferences that holds the
         single-active-session claim, whose AES master key lives in the hardware
         AndroidKeyStore and CANNOT be backed up — becomes part of the
         backup/restore/device-transfer set. On Samsung especially (Samsung Cloud
         auto-backup, Smart Switch, D2D transfer all fire invisibly), a restore
         lays the encrypted prefs back down without a matching keystore key and
         re-seeds a stale Auth store: reads fail, Firebase drops the session, and
         the user lands back on Login after a cold start — the exact iOS-fine /
         Android-signs-out symptom. Turning Auth persistence's storage over to
         the backup transport is the documented cause; disabling backup for this
         internal, sensitive ops app is the fix (session data does not belong in
         cloud backups regardless). `tools:replace` forces this to win over any
         library that merges in `allowBackup=true`. -->
    <application
        android:label="OpsHub"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false"
        tools:replace="android:allowBackup">
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/default_notification_channel_id" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- image_cropper (uCrop) — the crop / rotate / flip editor activity.
             Required by the plugin on Android; the AppCompat light no-action-bar
             theme is the plugin's documented requirement (uCrop extends
             AppCompatActivity). -->
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
